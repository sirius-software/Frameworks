namespace Bridge.Google.Maps
{
    using Bridge;

    [External]
    [Enum(Emit.Name)]
    [Namespace("google.maps")]
    public enum ScaleControlStyle
    {
        [Name("DEFAULT")]
        Default
    }
}